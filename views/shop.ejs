<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title></title>
</head>

<body>
  <header class="main-header">
    <h1>Ye Shopee</h1>
    <nav class="main-heaer__nav">
      <ul>
        <li><a href="/">Shop</a></li>
        <li><a href="/add">Add Product</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <% if (products.length > 0) { %>
    <div>
      <% for (let product of products) { %>
      <artice>
        <header>
          <h2><%= product.title %></h2>
        </header>
        <div>
          <img src="">
        </div>
        <div>
          <h2>$</h2>
        </div>
        <div>
          <button>Add</button>
        </div>
      </artice>
      <% } %>
    </div>
    <% } else  { %>
    <h1>No products found</h1>
    <% } %>
  </main>
</body>

</html>